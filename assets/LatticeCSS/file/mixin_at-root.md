## at-root()

**描述 (Description):**

`at-root($parentSelector, $isChild)`

將當前選擇器提升到指定的父層選擇器 (`parent selector`) 下，並保持或合併子選擇器結構。

### 📦 參數 (Parameters)

| **參數名稱**          | **類型**    | **描述**                                                                                                           |
| ----------------- | --------- | ---------------------------------------------------------------------------------------------------------------- |
| `$parentSelector` | `String`  | **指定**的父層選擇器。                                                                                                    |
| `$isChild`        | `Boolean` | 是否將子選擇器直接合併到父層選擇器<br/>• **`true` (預設):** 保持空格，產生「父層 **>** 當前選擇器」的結構。<br/>• **`false`:** 直接合併，無空格，產生「父層當前選擇器」的結構。 |

### 📝 範例 (Examples)

#### 範例 1: `$isChild = true` (預設)

將 `.card` 提升到 `.wrapper` 下，中間以空格分隔，表示後代選擇器。

```scss
// SCSS
.card {
  @include at-root(".wrapper") { color: red; }
}
```

**生成 CSS:**

```css
// CSS
.wrapper .card {
  color: red;
}
```

#### 範例 2: `$isChild = false`

將 `.item` 直接合併到 `.list` 後面，形成 `.list.item`。

```scss
// SCSS
.item { 
  @include at-root(".list", false) { font-weight: bold; }
}
```

**生成 CSS:**

```css
// CSS
.list.item { 
  font-weight: bold;
}
```
