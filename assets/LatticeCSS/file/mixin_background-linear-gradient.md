## bg-lg()

**描述 (Description):**

`bg-lg($setting)`

設置線性漸層背景 (Linear Gradient Background)。

### 📦 參數 (Parameters)

| **參數名稱**          | **類型**    | **描述**                                                                                                             |
| ----------------- | --------- | ------------------------------------------------------------------------------------------------------------------ |
| `$setting` | `String`  | 漸層設定，格式為 `'角度, 顏色1, 顏色2,...'`。<br/>• 預設值: `'45deg, #f00, #00f'`|

### 📝 範例 (Examples)

#### SCSS 程式碼:
```scss
.element {
  // 使用 90度，從黃色 (#ff0) 漸變到青色 (#0ff)
  @include bg-lg('90deg, #ff0, #0ff');
}
```
#### 生成的 CSS (概念):
> 註：實際的 Mixin 實現會包含瀏覽器前綴（如 `-webkit-`），這裡只顯示核心 CSS 屬性。
```css
.element {
  background-image: linear-gradient(90deg, #ff0, #0ff);
}
```